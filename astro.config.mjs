import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages deploy config. Adjust base if you are deploying to a sub-path.
  site: 'https://kikkawa-chomu.github.io', 
  base: '/Mypage',
});
